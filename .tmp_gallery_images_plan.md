# Plan: Replace gallery images on /gallery

## Information gathered
- `/gallery` route renders `src/app/components/InstagramFeed.tsx`.
- `InstagramFeed.tsx` currently uses `FEED` array with **16 hardcoded Unsplash image URLs**.
- It also uses a background image: `'/src/imports/Nearby/kaya.png'` (already local).
- Local image folders exist under `src/imports/images/` and multiple property folders (e.g. `src/imports/mums/`, `Apple Mango/`, `diani pearl/`, `flamboyant/`, etc.).

## Plan
1. Update `src/app/components/InstagramFeed.tsx`:
   - Replace the `image` field for each of the 16 `FEED` items with local images from `src/imports/images/`.
   - Keep likes/comments/caption the same (or optionally map captions to match images later).
   - Leave the background image (`kaya.png`) unchanged.
2. Validate that each chosen local image path exists:
   - Use paths already present in repo listing (e.g. `src/imports/images/mums-2.jpg`, `applemango-1.jpg`, `bahari-bed.jpg`, etc.).
3. Run `npm run build` to ensure TypeScript + bundling passes.

## Dependent files
- `src/app/components/InstagramFeed.tsx`

## Followup steps
- `npm run build`
- Manually check `/gallery` in browser.

## Images to use (16)
- mums-1.jpg
- mums-2.jpg
- mums-3.jpg
- mums-bed.jpg
- applemango-1.jpg
- applemango-2.jpg
- apple-bed.jpg
- bahari-1.jpg
- bahari-bed.jpg
- coral-1.jpg
- coral-2.jpg
- coral-bed.jpg
- dianipearl-1.jpg
- dianipearl-2.jpg
- pearl-bed.jpg
- flamboyant-1.jpg (or another available one if needed)

