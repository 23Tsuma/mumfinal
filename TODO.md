# TODO

## Gallery image replacement
- [x] Identify which gallery page/section to update (currently `/gallery` route uses `InstagramFeed` component)
- [x] Replace existing gallery images (currently hardcoded Unsplash URLs in `InstagramFeed.tsx`) with local images from `src/imports/...`

- [x] Use local folder images for all gallery thumbnails (16 items)

- [x] Ensure paths work with Vite (use absolute `/src/imports/...` pattern already used elsewhere)
- [x] Run `npm test` / `npm run build` (or `npm run dev`) to verify no TS/compile errors



